import { Routes } from '@angular/router';
import { SolarMapComponent } from './components/solar-map/solar-map.component';

export const routes: Routes = [
    { path: '', component: SolarMapComponent } // Sets the map as the main page
];